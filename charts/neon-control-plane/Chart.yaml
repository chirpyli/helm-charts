apiVersion: v2
name: neon-control-plane
description: Neon 最小控制面（tenant/endpoint 管理 API + storage-controller 调度入口）
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: "^1.18.x-x"
home: https://neon.tech
sources:
  - https://github.com/neondatabase/neon
