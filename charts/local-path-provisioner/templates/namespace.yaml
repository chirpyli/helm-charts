{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "local-path-provisioner.namespace" . }}
  labels:
    {{- include "local-path-provisioner.labels" . | nindent 4 }}
{{- end }}
