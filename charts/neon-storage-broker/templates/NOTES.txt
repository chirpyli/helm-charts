Storage Broker 已在集群内部署完成。

集群内访问地址：http://{{ include "neon-storage-broker.fullname" . }}:{{ .Values.service.port }}
{{- if .Values.ingress.enabled }}

外部访问入口（Ingress 已启用）：
{{- range .Values.ingress.hosts }}
  http://{{ .host }}{{ (index .paths 0).path }}
{{- end }}
{{- else }}

集群外访问需通过 Ingress，当前默认关闭（ingress.enabled=false）。
如需从集群外部调试 broker，请在 values.yaml 中启用 ingress 并配置 HTTP/2（gRPC）后端协议。
{{- end }}
